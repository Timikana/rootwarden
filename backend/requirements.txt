# Patch A06-01 (OWASP A06 Vulnerable & Outdated Components) :
# versions epinglees == au lieu de >=. Builds reproductibles, surface
# supply chain reduite (un push malicieux upstream n'est plus tire auto
# au prochain docker build).
#
# Pour mettre a jour : faire `pip install -U <pkg>` dans le container,
# tester, puis copier les versions resolues ici via `pip freeze`.
# Idealement : passer a pip-tools (requirements.in -> requirements.txt
# avec --generate-hashes) en suivi de ce ticket.

flask==3.1.3
flask-cors==6.0.2
werkzeug==3.1.8
mysql-connector-python==9.6.0
paramiko==5.0.0
pycryptodome==3.23.0
cryptography==47.0.0
marshmallow==4.3.0
hypercorn==0.18.0
paramiko-expect==0.3.5
pynacl==1.6.2
requests==2.33.1
croniter==6.2.2
pytest==9.0.3
pytest-cov==7.1.0
quart==0.20.0
quart-cors==0.8.0
